import React from 'react';
import { FormattedMessage } from 'react-intl';
import { shallow } from 'enzyme';

import RentEstate from '../index';

describe('<RentEstate />', () => {
  it('should render the page message', () => {
  });
});
