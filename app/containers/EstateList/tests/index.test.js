import React from 'react';
import { shallow } from 'enzyme';
import { Route } from 'react-router-dom';

import EstateList from '../index';

describe('<EstateList />', () => {
  it('should render some routes', () => {
  });
});
