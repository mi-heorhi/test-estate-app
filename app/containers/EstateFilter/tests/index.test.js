import React from 'react';
import { shallow } from 'enzyme';
import { Route } from 'react-router-dom';

import EstateFilter from '../index';

describe('<EstateFilter />', () => {
  it('should render some routes', () => {
  });
});
